import { SmoothHeightAnimDirective } from './smooth-height.directive';

describe('SmoothHeightAnimDirective', () => {
  it('should create an instance', () => {
    var x;
    const directive = new SmoothHeightAnimDirective(x);
    expect(directive).toBeTruthy();
  });
});
